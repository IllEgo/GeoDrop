# Kithe — Data Inventory (Task 0.2)

Companion to `docs/feature-inventory.md` (0.1) and `docs/migration-plan.md`. This is the
**Phase 0 data ground-truth**: counts of existing production records that the launch
scope disallows, broken out by collection, with date ranges and real-vs-test
attribution. It is the input to the 0.3 disposition ADR and the 1.4 data cleanup.

- **Project:** `geodrop-dfcba` (production)
- **Snapshot:** 2026-07-25 (generated by `functions/scripts/data-inventory.js`)
- **Method:** Read-only. The script performs no writes — no `--apply`, no mutations to
  Firestore/Storage/Auth. Re-runnable via `npm --prefix functions run data:inventory`.

## Provenance — real users vs. own testing

**Confirmed: 100% of this data is the owner's own testing — zero real third-party
users.** All 11 accounts that authored drops are the owner's primary account, the owner's
business (E3HI), the owner's alternate accounts, or throwaway test accounts. The owner
confirmed on 2026-07-25 that `robertp8@hawaii.edu` and `micahdiscord29@gmail.com` are
also their own accounts. **Implication:** the 0.3 disposition decisions carry no
real-user-data risk — nothing below belongs to an external party.

| Account | Providers | Drops | Read |
|---|---|---|---|
| robertmicahperalta@gmail.com | google, password | 68 | Owner (primary) |
| dog@dog.com | password | 7 | Test |
| test@gmail.com | password | 5 | Test |
| e3hi@gmail.com | password | 5 | Owner's business (E3HI) |
| robertp8@hawaii.edu | google | 4 | Owner alt (confirmed) |
| micahdiscord29@gmail.com | google | 3 | Owner alt (confirmed) |
| pig@pig.com | password | 3 | Test |
| cat@cat.com | password | 2 | Test |
| micah@e3hi.com | google | 1 | Owner's business (E3HI) |
| clothes@gmail.com | password | 1 | Test |
| history@bla.com | password | 1 | Test |

All 100 drops have a resolvable `createdBy` — **no anonymously-authored drops exist**,
confirming 0.1's finding that anonymous auth was never used.

## Totals by collection

Root collections present (5): `drops`, `groups`, `reports`, `usernames`, `users`.
**No DM/thread/conversation collection and no location-history/trail collection exist** —
confirming those features are structurally absent, not just unused.

| Collection | Count | Date range (createdAt) |
|---|---|---|
| `drops` | 100 | 2025-10-04 → 2026-07-25 |
| `users` (profiles) | 25 | — |
| Auth users | 22 | — |
| `groups` | 9 | 2025-10-20 → 2025-11-16 |
| `users/*/groups` (membership docs) | 24 ¹ | — |
| `reports` | 3 | — |
| `usernames` | 10 | — |

Drop content types: **TEXT 34, PHOTO 58, VIDEO 8** (= 100).

> ¹ **Corrected 2026-07-25 at the 1.4 dry run: 33 → 24.** The original figure came from
> `collectionGroup("groups")`, which matches the root `groups` collection as well as the
> per-user `users/*/groups` subcollection of the same name, so the 9 group documents were
> counted twice over (9 + 24 = 33). Verified against production: root `groups` = 9,
> `users/*/groups` walked per user = 24, `collectionGroup("groups")` = 33. The 1.4 wipe
> script walks each user document specifically to avoid this ambiguity. The group-record
> counts elsewhere in this document (9 groups, 6 group-scoped drops) are unaffected.

## Records the launch scope disallows

Counts with date ranges, each tied to its 0.1 classification and the phase that handles
disposition. **Note:** "disposition" ≠ "delete" for most of these — see the column.

| Category | Count | Date range | 0.1 class | Disposition (for 0.3 ADR) |
|---|---|---|---|---|
| Anonymous-posting toggle drops (`isAnonymous==true`) | **2** | 2025-10-08 (both) | Deferred (remove toggle) | Keep the drops; clear the `isAnonymous` flag so the creator's display name shows. Not deletion. |
| Video drops (`contentType==VIDEO`) | **8** | 2025-10-05 → 2025-11-16 | Deferred (Phase 2.3) | Remove video media + drop records (or downgrade). Also delete matching `drops/videos/…` Storage objects. |
| NSFW-flagged drops (`isNsfw`/`nsfw`/`nsfwLabels`) | **4** | 2025-10-07 → 2025-11-26 | Deferred (Phase 2.2) | Disposition per 0.3 — remove or quarantine. `backfill-launch-fields.js --quarantine-unsafe-media` already exists for this. |
| Drops carrying dislike data (`dislikeCount>0`/`dislikedBy`) | **2** | 2025-10-05 → 2025-10-18 | Deferred (Phase 2.6) | Keep the drops; strip `dislikeCount`/`dislikedBy` fields. Likes retained. |
| NSFW-enabled user prefs (`nsfwEnabled!=false`) | **3** | — | Deferred | Force `nsfwEnabled=false` / delete `nsfwEnabledAt`. Rules already enforce; `backfill-launch-fields.js` covers it. |
| DM threads | **0** | — | Deferred·absent | Nothing to dispose. |
| Stored location histories | **0** | — | N/A·absent | No trail collection. Drop `lat`/`lng` is content placement, not user tracking. |

### Group records — reclassified, NOT a disposal target

The 0.2 task template pre-listed "group records" as disallowed, but **0.1 Decision A
reclassified invite-only groups as launch scope** (the "explicitly joined experience"
mechanism). So these are **retained**, listed here for completeness:

- `groups`: **9** records (2025-10-20 → 2025-11-16)
- `users/*/groups` membership docs: **33**
- Group-scoped drops (`visibility==GROUP`): **6** (2025-10-10 → 2025-11-16)

No group in the schema is public/discoverable (all invite-code). The deferred item is
*public* groups, of which there are **0**. Follow-up from 0.1 stands: gate group
*creation* to organizer/business accounts.

## Integrity flags (for later phases)

- **25 user profiles vs. 22 Auth users** — at least 3 profile docs have no live Auth
  user (orphans), or vice-versa. Worth reconciling during the account-model work
  (Phase 2.7) and account-deletion audit.
  **2.7 (2026-07-29):** `npm --prefix functions run roles:check` now reports both
  directions of this mismatch (read-only, alongside the role normalization plan). It
  deletes nothing: an orphan profile is harmless because nobody can sign in as it, and a
  missing profile is created on next sign-in. Awaiting the prod dry run, which needs
  credentials.
- **Only 10 usernames** claimed across 22–25 users — expected (usernames are optional),
  noted so the count isn't mistaken for a mismatch.
- **`reports`: 3** — moderation data, retained. Confirms report/block has been exercised.

## Cross-check

100 drops fully attributed across 11 creators (68+7+5+5+4+3+3+2+1+1+1 = 100 ✓) and fully
typed (34+58+8 = 100 ✓). Disallowed counts are subsets of the 100 and do not overlap by
definition except where a drop is both (e.g., a video that is also NSFW would appear in
both rows) — spot disposition in 0.3 should de-dupe media deletions across the video and
NSFW sets.

## Gate

Per the migration plan, 0.2 stops here — **read-only, no disposition performed.** Please:

1. Confirm the provenance read above (especially `robertp8@hawaii.edu` and
   `micahdiscord29@gmail.com` — your accounts or external?).
2. Confirm these counts look right before we write the 0.3 disposition ADR.

Next task is **0.3 — Migration decision record** (delete vs. gate the code; delete,
orphan, or archive the data), which turns each row above into an explicit decision.
