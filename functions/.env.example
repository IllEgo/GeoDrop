# Required before deploying legal-consent callables. This must be an approved
# HTTPS origin owned by the GeoDrop mobile-app operator. The backend appends:
# /terms, /privacy, /community-guidelines, /promotion-terms, /data-retention,
# /subprocessors, /minors, and /support.
GEODROP_POLICY_BASE_URL=https://policies.example.invalid

